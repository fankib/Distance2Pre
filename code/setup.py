# how to install:
# run `pip install -e .` from this directory

from setuptools import setup

setup(name='public',
      version='0.1',
      description='public parts of Distance2Pre',
      author='.',
      author_email='.@.com',
      license='MIT',
      packages=['public'],
      zip_safe=False)
